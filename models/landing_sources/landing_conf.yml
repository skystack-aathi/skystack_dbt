version: 2

sources:
  - name: landing
    database: skystack_dbt_project
    schema: skystack_dbt_landing
    tables:
      - name: orders
        columns:
          - name: id
            data_tests:
              - unique:
                  config:
                    severity: warn
              - not_null:
                  config:
                    severity: warn    
          - name: product_id
            data_tests:
              - unique:
                  config:
                    severity: warn
                            
      - name: products
        columns:
          - name: id
            data_tests:
              - unique:
                  config:
                    severity: warn
              - not_null:
                  config:
                    severity: warn
                  
      - name: reviews
        columns:
          - name: id
            data_tests:
              - unique:
                  config:
                    severity: warn
              - not_null:
                  config:
                    severity: warn
      - name: users
        columns:
          - name: id
            data_tests:
              - unique:
                  config:
                    severity: warn
              - not_null:
                  config: 
                    severity: warn